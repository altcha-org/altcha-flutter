name: altcha_widget_example
description: Example app for the altcha_widget Flutter package.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  altcha_lib: ^0.2.0
  altcha_widget:
    path: ../
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
